//Steps are creating a project into firebase;
/*
 1.Create a project(Select a reliable name);
 2.Register app;
 3.npm install firebase**;
 4.initialized firebase into firebase.init.js;and export default app;
 5.Authentication (adding sign-in desire method and enable provider);
 6.const auth=getAuth(app) and import {getAuth} from "firebase/auth";
 7.react-bootsrap...........;
 8.
 */